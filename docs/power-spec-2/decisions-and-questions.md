# Local implementation decisions and open questions

These are direct Codex development decisions under the owner's 2026-09-22 instruction. They do not replace the missing normative release, grant qualification, or authorize Bob/model/hardware activity. All fixtures are public and synthetic unless a receipt explicitly establishes another role.

| ID | Slice | Conservative development decision | Remaining authority / limit |
| --- | --- | --- | --- |
| L001 | Bob fix | Explicit billing/authentication diagnostics are classified before model identity; unknown synthetic errors remain generic provider errors. Failed reservations stay consumed/uncertain. | Funded live validation remains R01. No old ledger is reset. |
| L002 | S05 | Existing `third_party/work/ppat-development` hosts the new public modules until S00. Reuse selected NumPy 2.3.5/SciPy 1.18.1 and the existing pytest/Hypothesis subset. | This installed-environment reuse is not portable closure; D02 must retain exact archives and rebuild cleanly. |
| L003 | S05 | Interval diagnostics report case-weighted and equal-block coverage, width and subgroups; caller supplies intervals. Score uncertainty uses SciPy percentile bootstrap of equal-block means, with explicit seed/resample count/confidence. Existing paired contrast math retains covariance and identity joins. | Independent sampling units, observation noise, model parameter draws, sharpness and qualification policy must come from approved evidence. No numerical acceptance limits are guessed. |
| L004 | S05 | Linear synthetic calibration uses SciPy least squares with caller-specified rank tolerance. Rank deficiency exposes residuals but no physically exclusive coefficients; grouping requires an explicit new design. OLS covariance is conditional on independent homoscedastic residuals and available residual degrees of freedom. | No AMD coefficients or physical calibration are inferred. Lineage checks enforce declared identities; they cannot authenticate undisclosed duplicates. Real calibration/discrepancy validation remains K-02. |
| L005 | S05 | Exactly one declared shared baseline plus baseline-free disjoint increments, canonical joules only. | IDs and kinds express ownership; physical boundary/overlap evidence is external. Already-baseline-inclusive components are unsupported. |
| L006 | S05 | Region composition supports additive sequential early/middle/late decode regions in every declared context. Explicit mappings cover every region exactly once, within phase/context, weighted by frozen work counts. No measured target duration enters reconstruction. | Manifest completeness and representativeness require independent C-02 checks. Overlap/general region schedulers, derivatives, approximate inference and multifidelity model fitting are not selected. Shared source IDs are disclosed in descriptive error/cost reports; they do not establish independent physical evidence. |
| L007 | S00 | Select `application/` as a reversible local development root and `power_spec2` namespace, with write surface `application/`. Preserve all original scratch/provider trees; reuse public modules through a source-hashed migration. Distribution is private development `power-spec2-development==0.0.1.dev0`, Python >=3.13, with existing selected numerical/test versions. | Not controller admission, target-platform selection or publishing/license authority. Owner may relocate without changing scientific contracts. |
| L008 | S00 | Preserve the original five unit tests and fixture byte-for-byte through a narrow `power_sim.contracts.unit_profile` compatibility import; preserve the unit implementation in the new namespace. | The temperature enum is dimensional legacy compatibility only; thermals remain excluded. |
| L009 | S00 | Nine supplied spec files are byte-hashed and relocated links crosswalked; 19 missing authority entries (18 unresolved referenced links) stay missing. An older `power-spec/normative-contract.md` exists but is not adopted as spec-2 authority. New contracts will use explicit development schema/status names. | Complete new normative schemas, evidence profiles and scientific status vocabulary remain owner/release blockers. No final compatibility or qualification claim. |
| L010 | S01 | Use jsonschema 4.26.0 and strict JSON only; six local document types use `power-spec2.dev.*.v1`. Physical roles, W/C profiles, final scoring tolerances/signatures and arbitrary transforms remain unsupported. Descriptive score profiles require null qualification policy, and reports cannot claim acceptance. | Full normative schema compatibility awaits the missing release. Local missing/unsupported/available states are explicitly development states, not substitutions for scientific verdicts. |
| L011 | S01 | Only explicit scalar declared-request/architecture features with a complete allowed public-input ancestry path can be projected. Construct fresh deterministic bytes; omit raw hashes, labels, native paths, display names and unused fields. Only identity transform, ephemeral retention and no capabilities are supported. | Ancestry is declared metadata, not authenticated evidence or OS isolation. Actual predictor custody and comprehensive side-channel denial remain R00. Grouped splitting checks declared split groups and artifact ancestor identities; full lineage must be supplied. |
| L012 | S01 | Prediction validation binds all five parent/runner/attempt identities and exact frozen case/work/time/quantity coverage. Missing or unsupported outputs remain null with reasons. Artifact reads reject symlinks, hardlinks, traversal and digest drift under a trusted fixed root. | Data-only files are limited to 8 MiB for these fixtures; larger native artifacts need an explicitly bounded streaming adapter. S02 adds immutable submission/attempt recording. |
| L013 | S02 | Reuse strict artifact reads, standard SHA-256 and SQLite FULL-sync transactions. Publish immutable content with fsync plus an exclusive hardlink publication (no replacement); retain staged remnants after real process crashes. Freeze exact submission object closure, caller-selected finite attempt budget, parent identities and expected work coverage. | This is local software custody, not durable remote storage or protection from the same UID. Actual OS principal separation and trusted external history anchors remain R00. |
| L014 | S02 | Append started and one terminal event; starts consume budget even when launch fails or the process crashes. Bind native/normalized output, submission/input/provider/scorer/policy, runner and attempt. Retain invalid/partial bytes as explicitly non-prediction artifact hashes. Commands/environment hashes and durations are records supplied by the controller, not execution attestations. | No API can accept or sign a submission. Trusted signer/interface selection and externally anchored independent validation are unresolved; supplied fake signatures reject. The local hash chain is integrity checking, not a replacement signature scheme. |
| L015 | D00 | First-wave acquisition preserves exact existing package versions: 27 app/numerical/test wheels, 101 Bob wheels and four separate build-tool wheels. Prefer verified cached originals, otherwise fetch HTTPS PyPI files bound to metadata SHA-256/size and compatible tags; inspect full wheel RECORD and archive paths. | Acquisition does not admit every package in the 46-group discovery registry. Clean runtime closure is checked separately in D02/D01. |
| L016 | D00 | Retain CPython 3.13.12 (standalone release 20260211) and 3.14.2 (20251217), matching chosen interpreter versions; reuse native Claude 2.1.278 only after its official release checksum matches. Use the owner's minimal OpenSSL config for standalone Python. | No paid CLI/model invocation. Host Linux x86_64/glibc compatibility is the current scope; MI355 target ABI remains owner-supplied. |
| L017 | D00 | Retain available source archives and exact upstream commits where PyPI lacks sdists. A bounded ONNX Runtime download hit 256 MiB; preserve the partial failure and retry once with a declared 1 GiB bound. Hypothesis 6.156.1 source remains unresolved after no sdist and expected-tag 404; retain the original wheel and its licenses without claiming source rebuild closure. | Native source-build gitlinks/toolchains, exact reproducibility of upstream binary builds and complete target closure are not established by wheel/source acquisition. |
| L018 | D02 | Reconstruct CPython 3.13.12 from the exact standalone archive and install the 27-wheel combined environment offline with hashes, with no inherited site-packages or source overlays. Keep the four-wheel build environment separate. SALib is a selected synthetic/native reference, not an application dependency. | Host Linux x86_64/glibc operability only; target ABI and physical uncertainty adequacy remain external. Binary runtime reconstruction does not close the Hypothesis source-rebuild gap. |
| L019 | D01 | Reconstruct Bob with exact CPython 3.14.2 and 101 hashed runtime wheels, then install the fresh current-source wheel and separately locked pytest-asyncio 1.4.0 test closure. Retain the verified native CLI but execute only its version command. | Clean host installation and mocked/local executor regressions do not establish funded model identity, target ABI or isolation. Bob campaigns remain stopped pending owner funding. |
| L020 | D08 | Select optional DRAMPower 6.0.0 source-embedded DDR/LPDDR energy and MGPUSim 4.2.0 MI300A/gfx942 timing adapters. Preserve Driver and CommandProcessor separately; native energy aliases are not summed twice. Reexecute verified native binaries and source inventories, including relevant original layer/grid tests. | No HBM/MI355 or physical power inference. Existing eight-case checker remains exploratory; no redundant dense/grid application implementation is selected. PAPI would require device access and SALib already passed D02, so neither receives a new adapter. DRAMPower installed SDK defects and MCCL exclusion remain. Arithmetic comparison uses 1e-12 relative / 1e-24 J absolute, never a physical acceptance tolerance. |
| L021 | D03 | Select HWComponents commit e67d7f10ee7210d265300d092d25e1b5f82dafba as the development open-component API, with explicit model lists and installed discovery disabled. Compare only a local synthetic read/write model; no technology scaling. Retain Accelergy commit 6911d15686ee7efdceba7d95605102df4472ae3a as a rejected CLI candidate with usable bounded Python energy accounting. | Physical AMD model content remains unqualified/owner-supplied. Accelergy has one stale exception test and two failing hierarchy tests (plugin loader UnboundLocalError); pin setuptools 80.9.0 only for its deprecated pkg_resources test compatibility. HWComponents archive builds as upstream fallback version 1.0; exact commit and wheel hash, not PyPI version, identify it. |
| L022 | D06 | Use explicit native Trace Processor v57.2 (SHA-bound ELF from the Perfetto 0.58.2 upstream manifest), CLI queries only. Support a bounded development Chrome JSON subset: complete slices and scalar counters, declared microsecond input, native nanosecond output, caller-declared W/boundary/clock and window/gap limit. Reuse existing interval integration; retain gaps and require an observed closing sample. | No Python API runtime, RPC server, first-use downloader, clock-offset fit or real acquisition export is selected. The 1.5 ms gap limit is only this synthetic fixture policy; hardware timebase/completeness and other trace formats require target evidence. |
| L023 | D09 | Select the public EnergAIzer c816608e154cbdb441d4ab5a78242f5350021476 softmax bf16 (batch=1024, dim=1024) reference at 510/705/900/1410 MHz. Freeze its source/data/config identities and expose all target voltage, idle-power and calibration tables. Run exact upstream numerical modules through a local library wheel whose two package initializers defer optional imports. | Queries overlap exposed calibration tables: public reproduction only, never held-out/blind evidence. Torch CPU endpoint returned 403; full frontend remains unavailable. Original Python 3.10/unpinned environment is not reconstructed; selected Python 3.13/30-wheel environment is explicitly frozen. No AMD, FP8/decode or independently controlled HBM inference. |
| L024 | D09 | Preserve native timing milliseconds, uncapped estimated watts and cap-applied joules as distinct outputs; normalize with explicit units and record every error. Use upstream yz8 profile exactly, including its existing comments/parameters. | README describes A100 40GB PCIe while one bandwidth comment mentions SXM; this reproduction does not resolve physical-profile provenance. The public data download publishes no independent checksum; retained SHA256 is observed transport identity. V-04 qualification still requires canonical dependencies and independent validation. |
