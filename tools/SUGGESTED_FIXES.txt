# SUGGESTED_FIXES.txt — generated by auto_fix_known_failures.sh
# 2026-05-15 12:45:18
# Review each entry and apply manually if appropriate.

## Banned ops in acceptance-criteria YAML
The following lines in spec files use subprocess/os which are banned in acceptance criteria:
  /home/yelkhamr/dark-factory/bob4/examples/bootstrap_v0.3.yaml:
  62:      - "python: import subprocess; r=subprocess.run(['grep','-rn','open.*CLAUDE.md','src/bob/'],capture_output=True,text=True); assert 'sanitize_for_claude_md' in open([l.split(':')[0] for l in r.stdout.splitlines() if 'CLAUDE.md' in l][0]).read() if r.stdout else True"
  /home/yelkhamr/dark-factory/bob4/examples/bootstrap_v0.3.yaml:
  62:      - "python: import subprocess; r=subprocess.run(['grep','-rn','open.*CLAUDE.md','src/bob/'],capture_output=True,text=True); assert 'sanitize_for_claude_md' in open([l.split(':')[0] for l in r.stdout.splitlines() if 'CLAUDE.md' in l][0]).read() if r.stdout else True"
  189:      - "python: import json,subprocess; subprocess.run(['python','-m','tools.capability_matrix','--out','/tmp/cm.json'],check=True); m=json.load(open('/tmp/cm.json')); assert 'languages' in m and m['languages']['python']['supported'] is True"
  
  Fix: rewrite acceptance criteria to use only pure-python stdlib checks or
       provide pre-existing test fixtures instead of shell-out logic.


# End of SUGGESTED_FIXES.txt — 1 suggestion block(s)
